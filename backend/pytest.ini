[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
markers =
    integration: requires live database (db:5432 reachable, marked tests skipped by default)
filterwarnings =
    ignore::DeprecationWarning:pydantic
    ignore::pydantic.PydanticDeprecatedSince20
